import React from 'react';
import styled from "styled-components";

const Auth = () => {
    return (
        <Root>
            
        </Root>
    );
};

export default Auth;

const Root = styled.div`
  
`;